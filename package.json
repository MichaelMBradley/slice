{
  "name": "slice",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "wasm": "wasm-pack build ./logic --target web",
    "build": "yarn wasm && vite build && tsc",
    "preview": "yarn build && vite preview"
  },
  "devDependencies": {
    "prettier": "^3.3.3",
    "typescript": "^5.5.4",
    "vite": "^5.3.4",
    "vite-plugin-wasm-pack": "^0.1.12"
  }
}
